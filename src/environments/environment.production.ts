export const environment = {
    production: true,
    /** URL pública da API em produção (sem barra no final). */
    /*apiUrl: 'https://sgea.digital/api'*/
    apiUrl: '/api',
    ambiente: 'production',
    useSimpleAuth: true
  };
